# 🚀 Быстрый старт - RAG Bot с Yandex GPT

## Шаг 1: Установка зависимостей

```bash
# Создайте виртуальное окружение
python -m venv venv

# Активируйте окружение
# Windows:
venv\Scripts\activate
# Mac/Linux:
source venv/bin/activate

# Установите зависимости
pip install -r requirements.txt
```

## Шаг 2: Настройка .env файла

Создайте файл `.env` в корне проекта:

```env
TELEGRAM_TOKEN=ваш_токен_от_BotFather
YANDEX_API_KEY=ваш_yandex_api_key
YANDEX_FOLDER_ID=ваш_yandex_folder_id
```

### Как получить токены:

1. **Telegram Token:**
   - Откройте [@BotFather](https://t.me/BotFather) в Telegram
   - Отправьте `/newbot` и следуйте инструкциям
   - Скопируйте полученный токен

2. **Yandex API Key и Folder ID:**
   - Зарегистрируйтесь в [Yandex Cloud](https://cloud.yandex.ru/)
   - Создайте каталог (folder) в консоли
   - Создайте сервисный аккаунт с ролью `ai.languageModels.user`
   - Создайте API ключ для сервисного аккаунта
   - Скопируйте Folder ID из URL или настроек каталога

## Шаг 3: Запуск бота

```bash
python main.py
```

## Шаг 4: Первое использование

1. Найдите вашего бота в Telegram
2. Отправьте `/start`
3. Отправьте `/ingest` для индексации документов
4. Задайте вопрос: "Какой у вас график работы?"

## 📝 Основные команды

- `/start` - Приветствие
- `/help` - Справка
- `/ingest` - Индексация документов
- `/ask <вопрос>` - RAG-запрос
- `/stats` - Статистика
- `/clear` - Очистить историю

## 🐛 Решение проблем

**Ошибка: "YANDEX_API_KEY не установлен"**
- Проверьте файл `.env` в корне проекта
- Убедитесь, что файл называется именно `.env` (не `.env.txt`)

**Ошибка: "База знаний не загружена"**
- Выполните команду `/ingest` в боте

**Ошибка при индексации:**
- Проверьте, что файлы в `data/docs/` имеют расширение `.txt`
- Проверьте кодировку файлов (должна быть UTF-8)

## 📚 Документация

Подробная документация: [README_YANDEX.md](README_YANDEX.md)


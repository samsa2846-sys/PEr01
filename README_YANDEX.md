# Telegram RAG Bot - Версия с Yandex GPT

Полноценный проект Telegram-бота с RAG-функциональностью на основе Yandex GPT API.

## 📋 Описание проекта

Этот бот использует технологию RAG (Retrieval-Augmented Generation) для ответов на вопросы пользователей на основе корпоративной базы знаний. Вместо OpenAI API используется Yandex GPT и Yandex Embeddings API.

### Возможности бота:

✅ **RAG-запросы** - поиск и ответы на основе базы знаний  
✅ **Векторный поиск** - быстрый семантический поиск через FAISS  
✅ **Индексация документов** - автоматическое обновление базы знаний  
✅ **Память разговоров** - контекстные ответы с учетом истории (до 10 сообщений)  
✅ **Логирование** - полное отслеживание всех операций  

## 📁 Структура проекта

```
PEr01/
├── main.py                    # Запуск бота
├── config.py                  # Конфигурация (Yandex настройки)
├── .env                       # Токены (создать!)
├── .env.example              # Пример настроек
├── requirements.txt          # Зависимости
├── data/docs/                # Документы для RAG
│   ├── company_info.txt      # Основной документ
│   ├── services.txt          # Услуги компании
│   └── contacts.txt          # Контакты
├── rag/                      # RAG система
│   ├── __init__.py
│   ├── yandex_embedder.py   # Создание эмбеддингов (Yandex)
│   ├── yandex_gpt.py         # Генерация ответов (Yandex GPT)
│   ├── vectorstore.py        # FAISS хранилище
│   ├── retriever.py          # Поиск документов
│   └── pipeline.py           # RAG пайплайн
└── README_YANDEX.md          # Эта документация
```

## 🚀 Установка и настройка

### Требования

- Python 3.10 или выше
- Telegram Bot Token (получить у @BotFather)
- Yandex Cloud API Key и Folder ID

### Шаг 1: Клонирование и установка зависимостей

```bash
# Клонируем проект (если еще не клонирован)
git clone https://github.com/samsa2846-sys/PEr01
cd PEr01

# Создаем виртуальное окружение
python -m venv venv

# Активируем окружение
# Для Windows:
venv\Scripts\activate
# Для Mac/Linux:
source venv/bin/activate

# Устанавливаем зависимости
pip install -r requirements.txt
```

### Шаг 2: Настройка переменных окружения

Создайте файл `.env` на основе `.env.example`:

```env
# Telegram Bot Token (получить у @BotFather)
TELEGRAM_TOKEN=your_telegram_bot_token_here

# Yandex Cloud API Settings
# Получить API ключ: https://console.cloud.yandex.ru/
# Получить Folder ID: https://console.cloud.yandex.ru/cloud
YANDEX_API_KEY=your_yandex_api_key_here
YANDEX_FOLDER_ID=your_yandex_folder_id_here
```

### Шаг 3: Получение Yandex Cloud credentials

1. Зарегистрируйтесь в [Yandex Cloud](https://cloud.yandex.ru/)
2. Создайте каталог (folder) в консоли
3. Получите API ключ:
   - Перейдите в раздел "Сервисные аккаунты"
   - Создайте сервисный аккаунт
   - Назначьте роль `ai.languageModels.user`
   - Создайте API ключ для сервисного аккаунта
4. Скопируйте Folder ID из URL консоли или из настроек каталога

### Шаг 4: Подготовка документов

Документы уже созданы в `data/docs/`:
- `company_info.txt` - основная информация о компании
- `services.txt` - детали услуг
- `contacts.txt` - контакты и реквизиты

Вы можете добавить свои `.txt` файлы в эту директорию.

## ▶️ Запуск бота

```bash
# Убедитесь, что виртуальное окружение активировано
python main.py
```

### Первый запуск

После запуска бота:

1. Найдите бота в Telegram по username
2. Отправьте команду `/start`
3. Выполните команду `/ingest` для индексации документов
4. Начните задавать вопросы!

## 💬 Использование

### Основные команды

| Команда | Описание |
|---------|----------|
| `/start` | Приветствие и основная информация |
| `/help` | Подробная справка по использованию |
| `/ask <вопрос>` | Задать вопрос с RAG-поиском |
| `/ingest` | Переиндексировать базу знаний |
| `/stats` | Показать статистику системы и истории |
| `/clear` | Очистить историю разговора |

### Способы взаимодействия

#### 1. Текстовые запросы

Просто напишите вопрос боту:

```
Какой у вас график работы?
```

Бот автоматически:
- Найдет релевантные документы в базе знаний
- Учтет историю предыдущих сообщений для контекста
- Сформирует контекст
- Сгенерирует ответ на основе найденной информации

#### 2. Команда /ask

Явный RAG-запрос:

```
/ask Какой у вас график работы?
```

## 🔧 Технические детали

### Архитектура RAG-системы

```
Пользователь → Telegram Bot
                    ↓
                RAG Pipeline
                    ↓
        ┌───────────┴───────────┐
        ↓                       ↓
    YandexEmbedder      DocumentRetriever
        ↓                       ↓
    Yandex API          VectorStore (FAISS)
        ↓                       ↓
    Embeddings          Search Results
        └───────────┬───────────┘
                    ↓
            Context Formation
                    ↓
            Yandex GPT
                    ↓
            Answer Generation
                    ↓
            Response to User
```

### Компоненты системы

#### 1. **YandexEmbedder** (yandex_embedder.py)

Преобразует текст в векторные представления (embeddings).

- Модель: text-search-doc
- Размерность: 256
- API: Yandex Cloud Foundation Models

#### 2. **VectorStore** (vectorstore.py)

Хранит и ищет векторные представления документов.

- Технология: FAISS (Facebook AI Similarity Search)
- IndexFlatL2 - евклидова метрика
- Сохранение на диск (.faiss + .json)

#### 3. **DocumentRetriever** (retriever.py)

Извлекает релевантные документы по запросу.

- Преобразование запроса в эмбеддинг
- Поиск Top-K похожих векторов
- Формирование контекста из найденных документов

#### 4. **RAGPipeline** (pipeline.py)

Координирует работу всех компонентов.

- Получение запроса пользователя
- Поиск релевантных документов (Retrieval)
- Формирование промпта с контекстом (Augmented)
- Генерация ответа моделью (Generation)

#### 5. **YandexGPT** (yandex_gpt.py)

Генерация ответов через Yandex GPT API.

- Модель: yandexgpt-lite
- Поддержка истории диалога
- Обработка системных промптов

### Используемые модели

| Модель | Назначение | Провайдер |
|--------|------------|-----------|
| text-search-doc | Создание эмбеддингов | Yandex Cloud |
| yandexgpt-lite | Генерация ответов | Yandex Cloud |

### Настройки RAG

В файле `config.py`:

```python
# Количество релевантных документов для контекста
TOP_K_RESULTS = 3

# Максимальная длина контекста
MAX_CONTEXT_LENGTH = 3000

# Модели
YANDEX_GPT_MODEL = "yandexgpt-lite"
YANDEX_EMBED_MODEL = "text-search-doc"
```

## 🔐 Безопасность

### Рекомендации:

1. **Не коммитьте файлы .env** в репозиторий
2. **Используйте .gitignore** для защиты секретов
3. **Ограничьте доступ** к команде /ingest (только администраторы)
4. **Логируйте все действия** для аудита
5. **Регулярно обновляйте** зависимости

## 🐛 Решение проблем

### Проблема: "База знаний не загружена"

**Решение:** Выполните команду `/ingest` для индексации документов

### Проблема: "Ошибка при обращении к Yandex API"

**Решение:** 
1. Проверьте API ключи в .env файле
2. Проверьте баланс аккаунта Yandex Cloud
3. Убедитесь, что сервисный аккаунт имеет роль `ai.languageModels.user`
4. Проверьте правильность Folder ID

### Проблема: "Документы не найдены"

**Решение:**
1. Убедитесь что .txt файлы находятся в `data/docs/`
2. Проверьте кодировку файлов (должна быть UTF-8)
3. Проверьте путь DOCS_PATH в config.py

### Проблема: "Ошибка импорта модулей"

**Решение:**
```bash
pip install -r requirements.txt --upgrade
```

## 📈 Масштабирование

### Для больших баз знаний:

1. **Используйте IVF индексы в FAISS:**
```python
# В vectorstore.py
self.index = faiss.IndexIVFFlat(quantizer, dimension, nlist)
```

2. **Разбивайте большие документы:**
```python
# Chunk size ~500-1000 слов
MAX_CHUNK_SIZE = 3000
```

3. **Используйте async операции:**
```python
# Для параллельной обработки запросов
import aiohttp
```

## 📝 Лицензия

Этот проект предоставлен в образовательных целях.

## 🔗 Полезные ссылки

- [Yandex Cloud Documentation](https://cloud.yandex.ru/docs/)
- [Yandex GPT API](https://cloud.yandex.ru/docs/ai-foundation-models/concepts/)
- [FAISS Documentation](https://github.com/facebookresearch/faiss)
- [Aiogram Documentation](https://docs.aiogram.dev/)
- [RAG Papers](https://arxiv.org/abs/2005.11401)

---

**Создано с ❤️ для демонстрации RAG технологии с Yandex GPT**

